import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#cfffd3" },
    secondary: { main: "#e6ffe8" },
    background: { default: "white", paper: "#eee" },
  },
});

export default theme;