import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#eee" },
    secondary: { main: "#002d04" },
    background: { default: "white", paper: "#eee" },
  },
});

export default theme;